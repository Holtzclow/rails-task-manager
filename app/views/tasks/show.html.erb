<h1><%= @task.title %></h1> <p><%= link_to "✏ edit this task", edit_path(@task)    %></p>
  <div class="container pt-3 border">
    <p><%= @task.details %></p></div>
    <%= form_for(@task) do |f| %>
    <%= f.check_box :completed %>
    <%= f.label :completed, @task.completed ? "task is completed" : "task is not completed" %>
    <%end%>
<%= link_to "Back to list", tasks_path %>
